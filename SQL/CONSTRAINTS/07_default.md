# Default Constraint

- It is a type of contraint we assign to a perticular column to step a default value.
- A column can only have only one values.
- Assigns a **default value** to a column
- Used when no value is provided during insertion

Examples:
- Default city = 'Pune'
- Default status = 'Active'